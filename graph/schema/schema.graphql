schema {
  query: Query
  mutation: Mutation
}

type Query {
  ping: String!
}

type Mutation {
  ping: String!
}

type RazorpayOrder {
  id: ID!
  amount: Int!
  currency: String!
}

extend type Mutation {
  createPaymentOrder(amount: Int!): RazorpayOrder!
}
