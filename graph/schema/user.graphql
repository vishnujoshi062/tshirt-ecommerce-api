type User {
  id: ID!
  email: String!
  name: String!
  phone: String
  address: String
  role: String!
  createdAt: String!
}

input RegisterInput {
  email: String!
  password: String!
  name: String!
  phone: String
  address: String
}

input LoginInput {
  email: String!
  password: String!
}

type AuthPayload {
  token: String!
  user: User!
}

extend type Query {
  me: User!
  getUser(id: ID!): User
}

extend type Mutation {
  updateProfile(name: String, phone: String, address: String): User!
}